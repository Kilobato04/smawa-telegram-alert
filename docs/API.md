# APIs de Telemetría (Pendiente)

En este documento se detallarán los endpoints, headers y formatos de respuesta (JSON) de las APIs que alimentarán el dashboard de las 4 cisternas.

## Estructura Esperada
- **Endpoint:** `[URL PENDIENTE]`
- **Método:** `GET`
- **Respuesta esperada:** Distancia actual (`distance_m`) y distancia de la lectura anterior (`prev_distance_m`) para calcular el gasto o recarga horario.
